# vehicle-manuals-archive
Private, ingestion-first content-addressable warehouse for OEM vehicle owner’s manuals. Autonomous discovery and scheduled crawling with per-site adapters; S3 (Amazon Simple Storage Service) object storage + Postgres metadata + OpenSearch full-text + vector index; robust PDF/HTML parsers and OCR (Optical Character Recognition);
